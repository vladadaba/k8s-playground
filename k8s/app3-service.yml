apiVersion: v1
kind: Service
metadata:
  name: app3
  labels:
    name: app3-service
    app: myapp
    type: backend
spec:
  selector:
    name: app3-pod
    app: myapp
    type: backend
  ports:
    - port: 3000
      targetPort: 3000
